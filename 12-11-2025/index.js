// 1. The Letter Grade Calculator
// Challenge: Convert a numerical score into a letter grade. Task:
// If score is 90 or above, log "A".
// If score is 80 to 89, log "B".
// If score is 70 to 79, log "C".
// If score is 60 to 69, log "D".
// If score is below 60, log "F


let score =95
if (score >= 90){
    console.log("A")
}
else if(score >= 89){
    console.log("B")
} 
else if (score >= 79){
    console.log("C")
}
else if (score >= 69){
    console.log("D")
}
else{
    console.log("F")
}


// 2nd sum 
// Challenge: Check a user's login credentials. You must check the username first. Only if the username is correct, you should then check the password. Task:
// Check if usernameAttempt matches correctUsername.
// If it does, then check if passwordAttempt matches correctPassword.
// If both are correct, log "Login Successful!"
// If the username was right but the password was wrong, log "Incorrect password."
// If the usernameAttempt was wrong in the first place, log "Invalid username."


